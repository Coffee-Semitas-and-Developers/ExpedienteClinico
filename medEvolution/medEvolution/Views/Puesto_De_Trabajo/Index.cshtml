@model IEnumerable<MedEvolution.Models.App.Puesto_De_Trabajo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Horario_De_Atencion.CodigoHorario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombrePuesto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Salario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Horario_De_Atencion.CodigoHorario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombrePuesto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Salario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodigoPuesto }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodigoPuesto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodigoPuesto })
        </td>
    </tr>
}

</table>
