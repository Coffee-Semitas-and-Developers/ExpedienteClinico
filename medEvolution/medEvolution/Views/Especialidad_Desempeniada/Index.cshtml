@model IEnumerable<MedEvolution.Models.App.Especialidad_Desempeniada>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreEspecialidad)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreEspecialidad)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.CodigoEspecialidad }) |
                @Html.ActionLink("Details", "Details", new { id=item.CodigoEspecialidad }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.CodigoEspecialidad })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
