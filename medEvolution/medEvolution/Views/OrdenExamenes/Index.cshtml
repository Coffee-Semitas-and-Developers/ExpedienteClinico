@model IEnumerable<medEvolution.Models.App.OrdenExamen>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Consulta.Sintomatología)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Urgencia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Resultado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaResultado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Consulta.Sintomatología)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Urgencia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Resultado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaResultado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdOrden }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdOrden }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdOrden })
        </td>
    </tr>
}

</table>
