@model IEnumerable<medEvolution.Models.App.DetalleExamenes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Examen.TipoMuestra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrdenExamen.IdOrden)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Examen.TipoMuestra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrdenExamen.IdOrden)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DetalleExamenesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DetalleExamenesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DetalleExamenesId })
        </td>
    </tr>
}

</table>
